# Скопируйте файл как server/.env и замените секрет перед публикацией сервера.
AUTH_SECRET=replace-with-a-long-random-secret
ACCESS_TOKEN_MINUTES=30
REFRESH_TOKEN_DAYS=30

# Необязательно. По умолчанию используется server/users.db.
# DATABASE_URL=sqlite:///server/users.db
